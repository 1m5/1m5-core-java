package network.onemfive.core;

/**
 * Notified when {@link ManConStatus} changes so hosts (a desktop UI, an Android
 * notification) can refresh what they show the user.
 */
public interface ManConStatusListener extends Runnable {
}
